name: mysociety
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore:
  image_picker:
  firebase_storage:
  firebase_auth:
  google_sign_in:
  geolocator:
  uuid:
  image:
  animator:
  path_provider:
  firebase_messaging:
  timeago:
  cached_network_image:
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
